- name: docker system prune {{ options }}
  shell: >
    docker system prune {{ options }}
  vars:
    options: ''
  register: prune_output

- debug:
    msg: '{{ prune_output.stdout_lines }}'
