- name: docker system prune {{ options | default('') }}
  shell: >
    docker system prune {{ options | default('') }}
  vars:
    options: ''
  register: prune_output

- debug:
    msg: '{{ prune_output.stdout_lines }}'
